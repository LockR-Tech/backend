server:
  port: ${SERVER_PORT:8087}
spring:
  application:
    name: notification-service
  cloud:
    openfeign:
      circuitbreaker:
        enabled: ${SPRING_CLOUD_OPENFEIGN_CIRCUITBREAKER_ENABLED:true}
        group:
          enabled: ${SPRING_CLOUD_OPENFEIGN_CIRCUITBREAKER_GROUP_ENABLED:true}
      client:
        config:
          default:
            connectTimeout: ${APP_FEIGN_CONNECT_TIMEOUT_MS:2000}
            readTimeout: ${APP_FEIGN_READ_TIMEOUT_MS:5000}
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:15432/notification_db}
    username: ${SPRING_DATASOURCE_USERNAME:notification_user}
    password: ${SPRING_DATASOURCE_PASSWORD:notification_pass}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: notification_schema
  flyway:
    enabled: true
    create-schemas: true
    schemas: notification_schema
    default-schema: notification_schema
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: ${SPRING_RABBITMQ_PORT:5672}
    username: ${SPRING_RABBITMQ_USERNAME:guest}
    password: ${SPRING_RABBITMQ_PASSWORD:guest}

app:
  security:
    jwt:
      secret: ${APP_SECURITY_JWT_SECRET:laundry-locker-microservices-secret-key-change-me-please-32chars}
  # Khoá service account Firebase (JSON một dòng) để gửi push. Là secret — nạp qua biến
  # môi trường. Trống thì push tắt, các kênh thông báo khác vẫn chạy.
  firebase:
    credentials-json: ${FIREBASE_CREDENTIALS_JSON:}
  # Địa chỉ gửi email. Một số nhà cung cấp bắt "From" phải là người gửi đã xác minh,
  # khác tài khoản SMTP; để trống thì dùng chính `spring.mail.username`.
  mail:
    from: ${APP_MAIL_FROM:}
  # Kênh SMS gửi mã mở tủ cho người nhận chưa có tài khoản.
  # KHOÁ LÀ SECRET — nạp qua biến môi trường trên VM, không commit giá trị vào repo.
  # Thiếu bất kỳ giá trị nào thì rơi về bản chỉ ghi log (service vẫn khởi động được).
  sms:
    twilio:
      account-sid: ${APP_SMS_TWILIO_ACCOUNT_SID:}
      auth-token: ${APP_SMS_TWILIO_AUTH_TOKEN:}
      from-number: ${APP_SMS_TWILIO_FROM_NUMBER:}
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,sbom
  endpoint:
    health:
      probes:
        enabled: true
  metrics:
    tags:
      application: ${spring.application.name}

springdoc:
  api-docs:
    enabled: ${SPRINGDOC_API_DOCS_ENABLED:true}

logging:
  pattern:
    level: "%5p [${spring.application.name:unknown},%X{correlationId:-}]"

resilience4j:
  circuitbreaker:
    configs:
      default:
        register-health-indicator: true
        sliding-window-size: ${APP_RESILIENCE4J_CB_SLIDING_WINDOW_SIZE:20}
        minimum-number-of-calls: ${APP_RESILIENCE4J_CB_MINIMUM_CALLS:10}
        failure-rate-threshold: ${APP_RESILIENCE4J_CB_FAILURE_RATE_THRESHOLD:50}
        wait-duration-in-open-state: ${APP_RESILIENCE4J_CB_WAIT_DURATION:30s}
        permitted-number-of-calls-in-half-open-state: ${APP_RESILIENCE4J_CB_HALF_OPEN_CALLS:3}
        slow-call-duration-threshold: ${APP_RESILIENCE4J_CB_SLOW_CALL_DURATION:4s}
        slow-call-rate-threshold: ${APP_RESILIENCE4J_CB_SLOW_CALL_RATE_THRESHOLD:80}
