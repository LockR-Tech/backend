<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <groupId>com.huynqb</groupId>
    <artifactId>laundry-locker-microservices</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>laundry-locker-microservices</name>

    <modules>
        <module>common-lib</module>
        <module>discovery-server</module>
        <module>api-gateway</module>
        <module>auth-service</module>
        <module>user-service</module>
        <module>order-service</module>
        <module>locker-service</module>

        <module>payment-service</module>
        <module>notification-service</module>
        <module>iot-service</module>
        <module>store-service</module>
        <module>loyalty-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2025.0.2</spring-cloud.version>
        <jjwt.version>0.13.0</jjwt.version>
        <springdoc.version>2.8.17</springdoc.version>
        <!-- Ghim vá CVE-2025-14813 (CRITICAL). spring-cloud-context kéo bcprov 1.80
             về theo đường transitive; 1.80.2 là bản vá cùng dòng. -->
        <bouncycastle.version>1.80.2</bouncycastle.version>
        <!-- Ghim vá CVE-2026-41293 (CRITICAL). Spring Boot 3.5.14 mang tomcat 10.1.54;
             ghi đè property này nâng cả embed-core, embed-el và embed-websocket. -->
        <tomcat.version>10.1.55</tomcat.version>
        <!-- Vá các CVE mức HIGH mà Trivy chặn. Tất cả đều là bản vá CÙNG DÒNG với
             bản Spring Boot 3.5.14 mang theo, nên chỉ là nâng patch chứ không đổi
             minor. Ghim ở đây thay vì từng service vì mọi thư viện dưới đây đều vào
             qua đường transitive, không service nào khai báo trực tiếp. -->
        <netty.version>4.1.136.Final</netty.version>              <!-- 4.1.132: 12 CVE, gồm netty-codec/-http/-http2/-dns, handler, resolver-dns -->
        <jackson-bom.version>2.21.4</jackson-bom.version>         <!-- 2.21.2: 3 CVE ở jackson-core và jackson-databind -->
        <spring-framework.version>6.2.19</spring-framework.version> <!-- 6.2.18: 3 CVE ở spring-expression, -webmvc, -webflux -->
        <spring-data-bom.version>2025.0.12</spring-data-bom.version> <!-- release train của spring-data-commons 3.5.12 -->
        <postgresql.version>42.7.12</postgresql.version>          <!-- 42.7.10: 3 CVE -->
        <micrometer.version>1.15.12</micrometer.version>          <!-- 1.15.11: 1 CVE -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>${bouncycastle.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webflux-api</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.cyclonedx</groupId>
                <artifactId>cyclonedx-maven-plugin</artifactId>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <executions>
                        <execution>
                            <goals>
                                <goal>build-info</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <parameters>true</parameters>
                        <release>${java.version}</release>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
